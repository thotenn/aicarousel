services:
  aicarousel:
    build: .
    ports:
      - "${PORT:-7123}:7123"
    volumes:
      - aicarousel-data:/app/data
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/aicarousel.db
    restart: unless-stopped

volumes:
  aicarousel-data:
