{
  "name": "aicarousel",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --watch run index.ts",
    "db:migrate": "bun run db/migrate.ts",
    "db:rollback": "bun run db/migrate.ts rollback",
    "api-key": "bun run scripts/api_key.ts",
    "api-key:create": "bun run scripts/api_key.ts create",
    "api-key:list": "bun run scripts/api_key.ts list"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@cerebras/cerebras_cloud_sdk": "^1.59.0",
    "@google/generative-ai": "^0.24.1",
    "@openrouter/sdk": "^0.3.11",
    "groq-sdk": "^0.37.0"
  }
}
